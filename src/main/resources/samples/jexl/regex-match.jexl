var hits = 0L;
var i = 0;
while (i < __ITER__) {
    var matcher = pattern.matcher(regexText);
    if (matcher.find()) {
        hits = hits + matcher.group(1).length() + matcher.group(2).length();
    }
    i = i + 1;
}
hits
